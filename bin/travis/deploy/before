#!/usr/bin/env bash

set -e
echo "> travis:deploy:before"

DEPLOY_GIT_NAME="Travis CI"
DEPLOY_GIT_EMAIL="builds@travis-ci.com"

git config user.name $DEPLOY_GIT_NAME
git config user.email $DEPLOY_GIT_EMAIL

npm run version:auto
